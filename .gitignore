# === 手冊附錄 A 規則一：資料絕對不可上傳到 GitHub ===
# 開頭的斜線很重要：沒有它的話 `data/` 會匹配任何層級的 data 目錄，
# 連 src/data/（程式碼）都會被忽略。加了斜線就只匹配 repo 根目錄那一個。
/data/
/models/
*.csv
*.tsv
*.zip
*.7z
*.parquet
*.feather
*.h5
*.pkl
*.joblib

# === 手冊附錄 A 規則二：Kaggle 憑證 ===
.kaggle/
kaggle.json

# === 機器專屬設定：路徑因機器而異，只有 .example 範本進 Git ===
configs/paths.yaml

# === 個人資料，不對外 ===
個人特質.txt

# === Python ===
__pycache__/
*.py[cod]
.venv/
venv/
.env
*.egg-info/
.ipynb_checkpoints/
.pytest_cache/
.ruff_cache/

# === 實驗追蹤 ===
mlruns/
wandb/
outputs/

# === 編輯器與 OS ===
.vscode/
.idea/
.DS_Store
Thumbs.db
